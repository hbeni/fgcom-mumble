# For most projects, this workflow file will not need changing; you simply need
# to commit it to your repository.
#
# You may wish to alter this file to override the set of languages analyzed,
# or to provide custom queries or build logic.
name: "CodeQL"

on:
  push:
    branches: [master]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [master]
  schedule:
    - cron: '0 21 * * 0'
  workflow_dispatch:

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        # Override automatic language detection by changing the below list
        # Supported options are ['csharp', 'cpp', 'go', 'java', 'javascript', 'python']
        language: ['cpp', 'java']
        # Learn more...
        # https://docs.github.com/en/github/finding-security-vulnerabilities-and-errors-in-your-code/configuring-code-scanning#overriding-automatic-language-detection

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      with:
        # We must fetch at least the immediate parents so that if this is
        # a pull request then we can checkout the head.
        fetch-depth: 2

    - if: matrix.language == 'java'
      name: Checkout GIT submodules (java)
      run: |
        git submodule init
        git submodule update client/radioGUI/lib/jsimconnect

    - if: matrix.language == 'java'
      name: Setup Java
      uses: actions/setup-java@v1
      with:
        java-version: '11' # The JDK version to make available on the path.

    - if: matrix.language == 'cpp'
      name: Setup C++
      run: sudo apt-get install libssl-dev mingw-w64 mingw-w64-common build-essential

    # Initializes the CodeQL tools for scanning.
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v1
      with:
        languages: ${{ matrix.language }}
        # If you wish to specify custom queries, you can do so here or in a config file.
        # By default, queries listed here will override any specified in a config file. 
        # Prefix the list here with "+" to use these queries and those in the config file.
        # queries: ./path/to/local/query, your-org/your-repo/queries@main

    - if: matrix.language == 'java' 
      name: Build Java
      run: |
        make radioGUI

    - if: matrix.language == 'cpp'
      name: Build C++ (Linux x64)
      run: |
        make clean test all

    - if: matrix.language == 'cpp'
      name: Build C++ (Windows x64 mingw-crosscompile)
      # skip openssl build, it takes long and will trigger false-positives
      run: |
        make SSLFLAGS= clean tools-win64 plugin-win64

    - if: matrix.language == 'cpp'
      name: Build C++ (Windows x32 mingw-crosscompile)
      # skip openssl build, it takes long and will trigger false-positives
      run: |
        make SSLFLAGS= clean plugin-win32


    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v1

    - if: matrix.language == 'cpp'
      name: 'Upload Artifact (plugin)'
      uses: actions/upload-artifact@v2
      with:
        name: FGCom-mumble_plugin-${{github.sha}}
        path: |
          client/mumble-plugin/fgcom-mumble*.so
          client/mumble-plugin/fgcom-mumble*.dll

    - if: matrix.language == 'java'
      name: 'Upload Artifact (radioGUI)'
      uses: actions/upload-artifact@v2
      with:
        name: FGCom-mumble_radioGUI-${{github.sha}}
        path: client/radioGUI/target/FGCom-mumble-radioGUI-*-jar-with-dependencies.jar

